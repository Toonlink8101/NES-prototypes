MEMORY {
    ZP:     start = $00,    size = $0100, type = rw, file = "";
    OAM:    start = $0200,  size = $0100, type = rw, file = "";
    RAM:    start = $0300,  size = $0500, type = rw, file = "";
    HDR:    start = $0000,  size = $0010, type = ro, file = %O, fill = yes, fillval = $00;
    ROM80:  start = $8000,  size = $7F00, type = ro, file = %O, fill = yes, fillval = $FF;
	ROMFF: 	start = $FF00,  size = $0100, type = ro, file = %O, fill = yes, fillval = $FF;
	CHR:    start = $0000,  size = $2000, type = ro, file = %O, fill = yes, fillval = $00;
}

SEGMENTS {
    ZEROPAGE: load = ZP,  type = zp;
    OAM:      load = OAM, type = bss, align = $100;
    BSS:      load = RAM, type = bss;
    HEADER:   load = HDR, type = ro;
	DMC:      load = ROMFF, type = ro, align = 64, optional = yes;
	CODE:     load = ROM80, type = ro, align = $100;
	RODATA:   load = ROM80, type = ro, align = $100;
    VECTORS:  load = ROMFF, type = ro,  start = $FFFA;
    TILES:    load = CHR, type = ro;
}
